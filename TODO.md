# TODO - Tela do fundo2 abaixo do fundo

- [ ] Entender como estão posicionadas as imagens `fundo` e `fundo2` em `index.html` e `fundo.css`.
- [ ] Ajustar CSS do `.fundo2` para ficar abaixo do `.fundo`, ocupando 100% da tela (top: 100vh; width/height 100%; object-fit: cover; z-index apropriado).
- [ ] Garantir que ambas fiquem atrás do conteúdo (z-index negativos) sem interferir em cliques.
- [ ] Rodar uma verificação rápida abrindo `index.html` no navegador para confirmar o layout.

